programa
{
	
	funcao inicio()
	{
		inteiro contador = 1
		inteiro soma = 0
		real media

		enquanto(contador <= 50)
		{
			se(contador % 2 != 0)
			{
				soma = soma + contador
			}
		contador++
		}
		media = soma / 25.0
		escreva(media)
	}
}
